# Beszel

The Hub starts with the base stack. The local agent is optional.

Create a token and system in Beszel, place the generated key and token in `.env`, then run:

```bash
make beszel-agent
```

Use `/beszel_socket/beszel.sock` as the agent endpoint in the Hub.
